# Tests

## `mscore` fixtures
- Generated score-meta fixtures live in `tests/mscore-meta/`. These fixtures are trimmed `mscore --score-meta` outputs used by MIDI parser tests.
- Regenerate them with: `bun scripts/generate-score-meta.ts`
